# NATS Server Configuration
# JetStream 配置 - 限制存储空间防止无限增长

jetstream {
  store_dir: /data
  
  # 文件存储限制 (持久化消息)
  max_file_store: 100G
  
  # 内存存储限制 (内存中的消息)
  max_memory_store: 512M
}

# 日志配置
debug: false
trace: false

# 监控端口
http_port: 8222
